= javascript_include_tag 'jquery.tagcloud.min', 'users_tagcloud'
.notes_board
  %h3
    Hall of Fame
  %ul#users_list
    - @users.each do |user|
      %li{:value => "#{user_points(user)}"}
        = link_to user.name, user_show_path(user)
